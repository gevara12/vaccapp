import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  separator: {
    paddingBottom: 16,
  },
  textInput: {
    borderWidth: 1,
    borderRadius: 4,
    marginBottom: 24,
    paddingVertical: 12,
    paddingHorizontal: 16,
  },
});
