import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    paddingVertical: 2,
    paddingHorizontal: 16,
    fontSize: 14,
    borderWidth: 1,
    borderRadius: 8,
  },
});
